import { ElementType } from "react";

export interface AdvantageProps {
  Icon: ElementType;
  title: string;
  description: string;
}

export interface ServiceProps {
  imgURL: string;
  name: string;
  desc: string;
}
